<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <include
        android:id="@+id/title_favorite"
        layout="@layout/favorite_title" />

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="54dp"
        android:background="@drawable/list_item_bg_normal" >

        <Button
            android:id="@+id/open_local_sgf"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_margin="6dp"
            android:background="@drawable/selector_video_item_layout"
            android:text="@string/open_local_sgf"
            android:textSize="20sp" />
    </RelativeLayout>

    <ExpandableListView
        android:id="@+id/listview_favorite"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/list_item_bg_normal"
        android:cacheColorHint="#00000000"
        android:dividerHeight="1dp"
        android:fadeScrollbars="true"
        android:fadingEdgeLength="0dp"
        android:listSelector="@drawable/list_item_bg_selected"
        android:paddingLeft="2dp"
        android:paddingRight="2dp" >
    </ExpandableListView>

</LinearLayout>